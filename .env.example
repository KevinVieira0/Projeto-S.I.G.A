# URL base da API do backend (NestJS, presumivelmente)
NEXT_PUBLIC_API_URL=http://localhost:3333

# Enquanto o endpoint real de validação de CNPJ não existe,
# deixe como "true" para usar o mock em src/lib/api/cnpjService.js
NEXT_PUBLIC_USE_CNPJ_MOCK=true
